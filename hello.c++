#include<iostream> 
using namespace std;
int main() {
   int age = 35;
   cout<<sizeof(age) << endl;
   //sizeof is an operator that returns the size of a variable in bytes
    cout << "Manan nagpal" << endl;
    return 0;
}

// this is the practice code